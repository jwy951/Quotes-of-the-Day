import { HihglightDirective } from './hihglight.directive';

describe('HihglightDirective', () => {
  it('should create an instance', () => {
    const directive = new HihglightDirective();
    expect(directive).toBeTruthy();
  });
});
